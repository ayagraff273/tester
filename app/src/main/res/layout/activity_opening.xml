<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/background_color"
    android:padding="20dp"
    android:id="@+id/opening"
    >


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/welcome"
        android:textSize="45sp"
        android:textStyle="bold"
        android:gravity="center"
        android:textColor="@color/primary_text"
        android:layout_marginBottom="20dp" />
    <ImageView
        android:id="@+id/shirtImage"
        android:contentDescription="@string/shirt_picture"
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:src="@drawable/shirt_bg"
        android:scaleType="fitCenter"
        android:layout_marginBottom="4dp"

        />

    <!-- תמונה של מכנסיים -->
    <ImageView
        android:id="@+id/pantsImage"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:src="@drawable/pants_bg5"
        android:scaleType="fitCenter"
        android:contentDescription="@string/pants_picture"
        android:layout_marginBottom="8dp"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/signin_or_signup"
        android:textSize="16sp"
        android:gravity="center"
        android:textColor="@color/secondary_text"
        android:layout_marginBottom="30dp" />

    <Button
        android:id="@+id/loginButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/sign_in"
        android:textSize="18sp"
        android:textColor="@color/white"
        android:backgroundTint="@color/button_color"
        android:layout_marginBottom="10dp"
        android:padding="12dp"
        tools:ignore="VisualLintButtonSize" />

    <Button
        android:id="@+id/registerButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/sign_up"
        android:textSize="18sp"
        android:textColor="@color/white"
        android:backgroundTint="@color/button_color"
        android:padding="12dp"
        tools:ignore="VisualLintButtonSize" />

</LinearLayout>

